{
  "name": "kwasa",
  "version": "1.0.0",
  "license": "MIT",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf ./dist && rm -rf ./.parcel-cache && parcel build"
  },
  "devDependencies": {
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "three": "^0.149.0"
  }
}
